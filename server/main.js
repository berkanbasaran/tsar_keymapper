// Server-side event handlers and logic can be added here
onNet('tsar_keymapper:serverEvent', (data) => {
    // Handle server-side events
});

// Example of registering a server export
global.ExampleServerExport = () => {
    // Server-side export function
}; 